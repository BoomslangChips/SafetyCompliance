@page "/companies/{CompanyId:int}"
@inject ICompanyService CompanyService
@inject IPlantService PlantService
@inject NavigationManager Navigation

@if (_company is null)
{
    <p>Loading...</p>
}
else
{
    <div class="page-header">
        <div>
            <button class="btn btn-link" @onclick="GoBack">&larr; Back</button>
            <h1>@_company.Name</h1>
            @if (_company.Code is not null)
            {
                <span class="card-subtitle">@_company.Code</span>
            }
        </div>
        <button class="btn btn-primary" @onclick="() => _showCreatePlant = true">Add Plant</button>
    </div>

    @if (_company.PhotoBase64 is not null)
    {
        <div class="entity-photo">
            <img src="@($"data:image/jpeg;base64,{_company.PhotoBase64}")" alt="@_company.Name" />
        </div>
    }

    @if (_showCreatePlant)
    {
        <div class="modal-overlay" @onclick="() => _showCreatePlant = false">
            <div class="modal" @onclick:stopPropagation>
                <h2>Add Plant</h2>
                <EditForm Model="_plantCreate" OnValidSubmit="CreatePlant">
                    <div class="form-group">
                        <label>Name</label>
                        <InputText @bind-Value="_plantCreate.Name" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label>Description</label>
                        <InputText @bind-Value="_plantCreate.Description" class="form-control" />
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Contact Name</label>
                            <InputText @bind-Value="_plantCreate.ContactName" class="form-control" />
                        </div>
                        <div class="form-group">
                            <label>Contact Phone</label>
                            <InputText @bind-Value="_plantCreate.ContactPhone" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Contact Email</label>
                        <InputText @bind-Value="_plantCreate.ContactEmail" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label>Photo</label>
                        <InputFile OnChange="OnCreatePlantPhotoSelected" class="form-control" accept="image/*" />
                        @if (_plantCreate.PhotoBase64 is not null)
                        {
                            <div class="photo-preview">
                                <img src="@($"data:image/jpeg;base64,{_plantCreate.PhotoBase64}")" alt="Preview" />
                                <button type="button" class="btn btn-sm btn-secondary" @onclick="() => { _plantCreate.PhotoBase64 = null; _plantCreate.PhotoFileName = null; }">Remove</button>
                            </div>
                        }
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Save</button>
                        <button type="button" class="btn btn-secondary" @onclick="() => _showCreatePlant = false">Cancel</button>
                    </div>
                </EditForm>
            </div>
        </div>
    }

    @if (_showEditPlant)
    {
        <div class="modal-overlay" @onclick="() => _showEditPlant = false">
            <div class="modal" @onclick:stopPropagation>
                <h2>Edit Plant</h2>
                <EditForm Model="_plantEdit" OnValidSubmit="UpdatePlant">
                    <div class="form-group">
                        <label>Name</label>
                        <InputText @bind-Value="_plantEdit.Name" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label>Description</label>
                        <InputText @bind-Value="_plantEdit.Description" class="form-control" />
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label>Contact Name</label>
                            <InputText @bind-Value="_plantEdit.ContactName" class="form-control" />
                        </div>
                        <div class="form-group">
                            <label>Contact Phone</label>
                            <InputText @bind-Value="_plantEdit.ContactPhone" class="form-control" />
                        </div>
                    </div>
                    <div class="form-group">
                        <label>Contact Email</label>
                        <InputText @bind-Value="_plantEdit.ContactEmail" class="form-control" />
                    </div>
                    <div class="form-group">
                        <label>Photo</label>
                        <InputFile OnChange="OnEditPlantPhotoSelected" class="form-control" accept="image/*" />
                        @if (_plantEdit.PhotoBase64 is not null)
                        {
                            <div class="photo-preview">
                                <img src="@($"data:image/jpeg;base64,{_plantEdit.PhotoBase64}")" alt="Preview" />
                                <button type="button" class="btn btn-sm btn-secondary" @onclick="() => { _plantEdit.PhotoBase64 = null; _plantEdit.PhotoFileName = null; }">Remove</button>
                            </div>
                        }
                    </div>
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Update</button>
                        <button type="button" class="btn btn-secondary" @onclick="() => _showEditPlant = false">Cancel</button>
                    </div>
                </EditForm>
            </div>
        </div>
    }

    <div class="card-list">
        @foreach (var plant in _plants)
        {
            <div class="card clickable" @onclick="() => GoToPlant(plant.Id)">
                @if (plant.PhotoBase64 is not null)
                {
                    <div class="card-photo">
                        <img src="@($"data:image/jpeg;base64,{plant.PhotoBase64}")" alt="@plant.Name" />
                    </div>
                }
                <div class="card-header-row">
                    <h3>@plant.Name</h3>
                    <div class="card-edit-btn" @onclick:stopPropagation>
                        <button class="btn btn-sm btn-secondary" @onclick="() => StartEditPlant(plant)">Edit</button>
                    </div>
                </div>
                <p class="card-subtitle">@(plant.ContactName ?? "No contact")</p>
                <div class="card-stats">
                    <span>@plant.SectionCount sections</span>
                    <span>@plant.EquipmentCount equipment</span>
                </div>
            </div>
        }

        @if (!_plants.Any())
        {
            <p class="empty-state">No plants yet. Add one to get started.</p>
        }
    </div>
}

@code {
    [Parameter] public int CompanyId { get; set; }

    private CompanyDto? _company;
    private List<PlantDto> _plants = [];
    private bool _showCreatePlant;
    private bool _showEditPlant;
    private int _editPlantId;
    private PlantCreateModel _plantCreate = new();
    private PlantEditModel _plantEdit = new();

    protected override async Task OnInitializedAsync() => await LoadData();

    private async Task LoadData()
    {
        _company = await CompanyService.GetCompanyByIdAsync(CompanyId);
        _plants = await PlantService.GetPlantsByCompanyAsync(CompanyId);
    }

    private async Task CreatePlant()
    {
        var dto = new PlantCreateDto(CompanyId, _plantCreate.Name, _plantCreate.Description, _plantCreate.ContactName, _plantCreate.ContactPhone, _plantCreate.ContactEmail, _plantCreate.PhotoBase64, _plantCreate.PhotoFileName);
        await PlantService.CreatePlantAsync(dto, "current-user");
        await LoadData();
        _showCreatePlant = false;
        _plantCreate = new();
    }

    private void StartEditPlant(PlantDto plant)
    {
        _editPlantId = plant.Id;
        _plantEdit = new PlantEditModel
        {
            Name = plant.Name,
            Description = plant.Description,
            ContactName = plant.ContactName,
            ContactPhone = plant.ContactPhone,
            ContactEmail = plant.ContactEmail,
            IsActive = plant.IsActive,
            PhotoBase64 = plant.PhotoBase64,
            PhotoFileName = plant.PhotoFileName
        };
        _showEditPlant = true;
    }

    private async Task UpdatePlant()
    {
        var dto = new PlantUpdateDto(_editPlantId, _plantEdit.Name, _plantEdit.Description, _plantEdit.ContactName, _plantEdit.ContactPhone, _plantEdit.ContactEmail, _plantEdit.IsActive, _plantEdit.PhotoBase64, _plantEdit.PhotoFileName);
        await PlantService.UpdatePlantAsync(dto, "current-user");
        await LoadData();
        _showEditPlant = false;
        _plantEdit = new();
    }

    private async Task OnCreatePlantPhotoSelected(InputFileChangeEventArgs e)
    {
        var file = e.File;
        if (file is null) return;
        var buffer = new byte[file.Size];
        await file.OpenReadStream(maxAllowedSize: 5 * 1024 * 1024).ReadAsync(buffer);
        _plantCreate.PhotoBase64 = Convert.ToBase64String(buffer);
        _plantCreate.PhotoFileName = file.Name;
    }

    private async Task OnEditPlantPhotoSelected(InputFileChangeEventArgs e)
    {
        var file = e.File;
        if (file is null) return;
        var buffer = new byte[file.Size];
        await file.OpenReadStream(maxAllowedSize: 5 * 1024 * 1024).ReadAsync(buffer);
        _plantEdit.PhotoBase64 = Convert.ToBase64String(buffer);
        _plantEdit.PhotoFileName = file.Name;
    }

    private void GoToPlant(int id) => Navigation.NavigateTo($"/plants/{id}");
    private void GoBack() => Navigation.NavigateTo("/companies");

    private class PlantCreateModel
    {
        public string Name { get; set; } = string.Empty;
        public string? Description { get; set; }
        public string? ContactName { get; set; }
        public string? ContactPhone { get; set; }
        public string? ContactEmail { get; set; }
        public string? PhotoBase64 { get; set; }
        public string? PhotoFileName { get; set; }
    }

    private class PlantEditModel
    {
        public string Name { get; set; } = string.Empty;
        public string? Description { get; set; }
        public string? ContactName { get; set; }
        public string? ContactPhone { get; set; }
        public string? ContactEmail { get; set; }
        public bool IsActive { get; set; } = true;
        public string? PhotoBase64 { get; set; }
        public string? PhotoFileName { get; set; }
    }
}
